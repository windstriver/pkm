#+title: Keywords AI

Learning Rate:
hyperparameter that determines how much the model updates its weights during each optimization step, balancing convergence speed and stability.

Training Batch:
a subset of the dataset used in a single forward and backward pass during training.

Training Epoch:
one complete pass through the entire training dataset
